/**
 * PendingContent — the visible marker for saint content awaiting the
 * Product Owner's text (FR-94; UXD v1.2 §20).
 */
export default function PendingContent() {
  return <p className="detail__nodata pending-content">[Content pending — to be provided]</p>;
}
